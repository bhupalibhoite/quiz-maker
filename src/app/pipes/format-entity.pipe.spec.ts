import { FormatEntityPipe } from './format-entity.pipe';

describe('FormatEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
