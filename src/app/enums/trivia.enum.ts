export enum DifficultyLevel {
    Easy = 'Easy',
    Medium = 'Medium',
    Hard = 'Hard',
  }
  
  export enum DataLimit {
      FIVE = '5'
  }
  
  export enum DataType {
      MULTIPLE = 'multiple'
  }
  
  export enum ColorCode {
    GREEN_CODE = 'btn-success',
    RED_CODE = 'btn-danger',
    NOTIFY_RED = 'score-notification-red',
    NOTIFY_GREEN = 'score-notification-green',
    NOTIFY_YELLOW = 'score-notification-yellow'
  }
  